# Dependencies, caches, and build output
node_modules/
dist/
dist-ssr/
.vite/
*.tsbuildinfo

# Local environment and credentials
.env
.env.*
!.env.example
*.local
game.config.playground.json
*.pem
*.key
*.p12

# RUN local/operator state (keep reviewed source configs elsewhere)
.rundot/
rundot-session*
player-snapshot*
campaign-state*

# Test and QA output. NOT a bare `screenshots/` — that also matched
# docs/screenshots/, so the README's gameplay image never reached GitHub and
# rendered as a broken link. QA output already lives under tmp/.
coverage/
playwright-report/
test-results/
/screenshots/
tmp/
!docs/screenshots/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editors and operating systems
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp
*.swo
*~
